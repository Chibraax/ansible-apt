---
- name: Update packages
  ansible.builtin.package:
    update_cache: true
  tags: apt

- name: "Role for install all required packages"
  ansible.builtin.apt:
    name: "{{ item }}"
    state: present
  loop: "{{ packages }}"
  tags: apt
