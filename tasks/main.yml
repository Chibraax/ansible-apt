---
- name: Update packages
  ansible.builtin.apt:
    update_cache: true
  tags: apt

- name: "Role for install all required packages"
  ansible.builtin.apt:
    name: "{{ item }}"
    state: present
  loop: "{{ packages }}"
  tags: apt
