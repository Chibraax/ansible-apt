---
- name: Update packages
  ansible.builtin.apt:
    update_cache: true

- name: "Role for install all required packages"
  ansible.builtin.apt:
    name: "{{ item }}"
    state: present
  loop: "{{ packages }}"
